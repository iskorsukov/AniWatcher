query SeasonAiringData($year: Int!, $season: MediaSeason!, $page: Int!) {
    Page(page: $page) {
        pageInfo {
            currentPage
            hasNextPage
        }
        media(season: $season, seasonYear: $year, sort: START_DATE, status_in: [RELEASING, NOT_YET_RELEASED]) {
            id
            title {
                romaji
                english
                native
            }
            description
            coverImage {
                medium
                color
            }
            rankings {
                type
                rank
                season
            }
            season
            meanScore
            genres
            siteUrl
            airingSchedule(notYetAired: true) {
                nodes {
                    id
                    airingAt
                    episode
                    mediaId
                }
            }
        }
    }
}